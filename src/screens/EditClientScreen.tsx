import { ScrollView } from "react-native"
import React from "react"
import EditClient from "../components/EditClientComponents/EditClient"


const EditClientScreen = () => {
  return (
      <ScrollView>
        <EditClient />
      </ScrollView>
  )
}

export default EditClientScreen
