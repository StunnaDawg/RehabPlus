import { ScrollView } from "react-native"
import React from "react"
import StartPage from "./NewProtocolScreens/StartPage"

const CreateNewProtocol = () => {
  return (
    <ScrollView>
      <StartPage />
    </ScrollView>
  )
}

export default CreateNewProtocol
