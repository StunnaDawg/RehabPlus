import { View, Text, ScrollView } from 'react-native'
import React from 'react'
import StartPage from '../components/NewProtocolComponents/StartPage'

const CreateNewProtocol = () => {
  return (
    <ScrollView>
      <StartPage />
    </ScrollView>
  )
}

export default CreateNewProtocol