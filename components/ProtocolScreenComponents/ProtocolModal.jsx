import { Modal } from "react-native-paper"

import { View, Text } from "react-native"
import React from "react"

const ProtocolModal = ({ protocolId }) => {
  return (
    <View>
      <Text>ProtocolModal: id {protocolId}</Text>
    </View>
  )
}

export default ProtocolModal
